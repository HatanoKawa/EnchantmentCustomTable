# 全版本分级 GUI 验证：第三轮进度记录

范围：`dev` 的 15 个 Minecraft 版本 × Fabric / NeoForge，共 30 个目标。
**这是未完成的阶段记录。** 2026-09-22 22:05 左右，Computer Use 返回 Mac 已锁定、无法自动解锁，
本记录保存该暂停点的结果。用户之后已解锁并要求继续，后续执行正在恢复。
全矩阵构建通过不代表全矩阵 GUI 通过。

## 分级与当前结果

[分级方案与用例](tiered-matrix.md)按实际兼容层划分：9 个版本全量、6 个版本快速。
全量为 28 个业务用例加 2 个渲染/输入检查；快速为 8 个组合场景，保留支付、复制、拆书和持久化。
快速组不运行配置组合，不把这些配置项记为通过；快速组发现问题后扩展对应场景验证。

| 版本 | 等级 | 构建/JVM 双端 | Fabric GUI | NeoForge GUI |
| --- | --- | --- | --- | --- |
| 1.21.1 | 全量 | 通过 | 复用第二轮 26 通过/2 阻塞；V01/V02 待补 | 同左 |
| 1.21.2 | 全量 | 通过 | 待验 | 待验 |
| 1.21.3 | 快速 | 通过 | 待验 | 待验 |
| 1.21.4 | 全量 | 通过 | 待验 | 待验 |
| 1.21.5 | 全量 | 通过 | 待验 | 待验 |
| 1.21.6 | 全量 | 通过 | 待验 | 待验 |
| 1.21.7 | 快速 | 通过 | 待验 | 待验 |
| 1.21.8 | 快速 | 通过 | 待验 | 待验 |
| 1.21.9 | 全量 | 通过 | 28 通过 / 2 阻塞 | 28 通过 / 2 阻塞 |
| 1.21.10 | 快速 | 通过 | 待验 | 待验 |
| 1.21.11 | 全量 | 通过 | 待验 | 待验 |
| 26.1 | 全量 | 通过 | 28 通过 / 2 阻塞 | 26 通过 / 2 阻塞 / K05、K06 待验 |
| 26.1.1 | 快速 | 通过 | 待验 | 待验 |
| 26.1.2 | 快速 | 通过 | 待验 | 待验 |
| 26.2 | 全量 | 通过 | 待验 | 待验 |

计数单位为用例 × 加载器，636 个计划检查中：本轮新执行 110 通过，复用第二轮 52 通过，
12 个 Shift 输入阻塞，其余 462 待验。后续全量目标的 Shift 项尚未执行登记，不提前累计阻塞。
3 个目标已完成约定清单的可执行部分；NeoForge 26.1 部分完成。
另有下述存档迁移失败，不在原 30/8 项的同版本回归计数内，不能被上述通过数掩盖。

## 代码、工具和构建

- `862106e`：将第二轮仅应用于 Fabric 1.21.1 的服务端复制保护扩展至另外七个活动版本层。
  客户端同步模板槽时不再误生成输出并消耗客户端材料。未改动未使用的 `modern_render` 层。
- `e0aa94e`：分级清单、全版本启动清单导出、按加载器/版本隔离的 macOS 应用和测试世界准备工具。
  修正 Loom 26.x 运行目录为 `FileSystemLocation` 的区别。
- 每目标使用 `ECT-GUI-R3-<loader>-<version>` 独立世界。数据包格式取自游戏 JAR 元数据，
  1.21.5+ 命令使用扁平附魔组件。数据包只准备材料/读取数据，业务操作均通过真实 GUI。
- 游戏使用 Java 21 / 25 对应工具链。未改系统 JDK、全局 Java 配置或常规发布入口，未推送远端。

在相同生产代码工作树上执行，之后提交为 `862106e`：

```sh
JAVA_HOME=/opt/homebrew/opt/openjdk/libexec/openjdk.jdk/Contents/Home \
  ./gradlew --no-daemon --no-configuration-cache --max-workers=1 -Dorg.gradle.jvmargs=-Xmx4g \
  -PneoForge.neoFormRuntime.launcherManifestUrl=file:///Users/river_quinn/.gradle/caches/neoformruntime/artifacts/minecraft_launcher_manifest.json \
  -I tools/gui-validation/export-client.init.gradle -PguiLoader=both -PguiVersion=all \
  :verifyAll :fabric_1_21_9:exportGuiClientLaunch :1.21.9:exportGuiClientLaunch
```

成功：289 个任务，约 2 分 10 秒。归档结果为 common 22 项、15 个 Fabric 目标各 40 项、
15 个 NeoForge 目标各 22 项，均无失败、错误、跳过，共 952 个 JVM 测试执行。
其余客户端随后导出；26.x 首次导出遇到运行目录类型差异，修正后重跑成功。
已逐一核对 30 份启动清单的加载器与 Minecraft 版本。

## 已完成的实际观察

1.21.9 双端及 26.1 Fabric 的全部可执行检查通过；NeoForge 26.1 除 K05/K06 外通过：

- 普通兑换 2 书/36 绿宝石后剩 1 书，保存重进后保持；领取前先暂停保存，覆盖已保存区块。
- 复制 3 书/12 块，生成和领取补位后剩 1 书/4 块，模板和待领取输出均 V 级；重进后与 GUI 一致。
- 搜索、无匹配、清空、分页、含 `e` 的输入和 Esc 重开正常；菜单打开时 reload 后仍可领取。
- 三轮拆分均为 III → II + I → III，末尾玩家无额外书；取出四个附魔后工具无附魔。
- 输入槽、空候选槽、同名槽插书后工具为 Sharpness II、Unbreaking II、Looting I；非法物品保留。
  候选排序随版本/加载器变化，操作前通过提示确认目标。
- 合并工具和四附魔整书导出分别保存重进，数量与组件一致。
- 严格模式 II + II → III，不同级/混合书原子拒绝，V + V 拒绝，新 Looting IV 可加入。
- 完成免费组的三个目标均取得 Sharpness I 和两本 Sharpness V，原 3 书/64 绿宝石保留。

Shift+鼠标仍缺少当前工具支持，普通点击不能替代 E02/E06。
NeoForge 1.21.9 Mods 列表崩溃沿用已有接受记录；本轮未进入 Mods 列表，未添加绕过补丁。

## 新发现：跨版本台内物品迁移失败，尚未修复

原第二轮 1.21.1 存档在 `(0,-60,2)` 的转换台槽 2、3 各有一本 Sharpness V。
只读解析原 region 文件及未启动的 26.2 副本，确认 `DataVersion=3955`、原数据确实存在。
原测试世界与日常存档未修改。

- **MIG-01，Fabric 1.21.1 → 1.21.9 / 26.1**：首次加载打印组件解码失败，旧
  `stored_enchantments={levels:{...}}` 无法被新组件解析。26.1 时间为 21:26:12，槽 2、3 均报错。
  精确丢失范围、其他组件和版本边界仍需专项验证。
- **MIG-02，NeoForge 1.21.1 → 26.1**：21:49:35 首次准备场景前读取台内数据，
  `Inventory.stacks` 四槽均为空，玩家附魔书却正常升级。原库存为 `Inventory.Size/Items`，
  当前资源处理器使用 `Inventory.stacks`。尚未实现兼容旧格式的迁移。

后续应独立覆盖两种台子、普通材料/多组件物品、迁移后再次保存重进；不能只做字段替换
便宣称支持全部升级路径。当前结果不能作为“保留台内物品直接升级世界安全”的依据。
同版本功能检查在记录初始异常后重新准备材料，未将迁移失败混入同版本持久化结果。

## 证据与继续执行

- [30 目标逐项结果和依赖](evidence/round3-2026-09-22/matrix-results.json)
- [JVM 结果](evidence/round3-2026-09-22/automated-checks.json)
- [迁移前台内数据](evidence/round3-2026-09-22/migration-source-inventories.json)
- [来源、筛选规则和 SHA-256](evidence/round3-2026-09-22/manifest.json)

同目录约 2.9 MB 的关键截图、配置和服务端记录。完整日志及其他截图位于
`build/reports/gui-validation/round3/`，包括尚未完成的 NeoForge 26.1 配置组。

锁屏时 NeoForge 26.1 停在免费配置的转换台查询界面，K05/K06 不记通过。
等待解锁期间先完成证据整理，之后只向本轮启动的该进程发送 SIGTERM（退出码 143），
未把这次终止记录为成功的 GUI 保存退出；该未完成场景需重新准备。
现在 30 个目标的配置与选项均已恢复：已有文件按字节比对，原本不存在的临时文件已删除。

继续时先重启 NeoForge 26.1 的 strict-free 配置，重新准备 efree，完成 K05/K06 并恢复原文件。
其他目标已有独立世界与启动清单，
无需再次复制；启动前重新应用默认测试配置与 GUI 选项（此前已恢复）：

```sh
python3 tools/gui-validation/prepare_matrix.py --loader fabric --minecraft 26.2 --config default
cp build/reports/gui-validation/round3/fabric/1.21.1/options-original fabric_versions/26.2/run/options.txt
python3 tools/gui-validation/macos_client.py --loader fabric --minecraft 26.2 --launch
```

随后优先 26.2 双端，再完成其余兼容层与快速组，最后补 1.21.1 的 V01/V02。
没有解锁后的实际画面与操作证据，不能将待验目标更新为通过。
